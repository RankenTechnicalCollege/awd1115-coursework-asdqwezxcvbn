@using Microsoft.AspNetCore.Identity
@using System.Security.Claims
@using S3FinalV2.Models
@inject UserManager<ApplicationUser> UserManager
@inject SignInManager<ApplicationUser> SignInManager

@{
    var year = DateTime.Now.Year;
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();
    var action = ViewContext.RouteData.Values["action"]?.ToString();
    var area = ViewContext.RouteData.Values["area"]?.ToString();
    var isAuth = User?.Identity?.IsAuthenticated == true;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MechTrack</title>

    <!-- Inter font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet" />

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Font Awesome (free) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark site-navbar mb-3">
            <div class="container-fluid">

                <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                    <img src="~/images/MechTrackLogos/MechTrackLogoRFB.png"
                         alt="MechTrack Logo" height="50" />
                </a>

                <button class="navbar-toggler" type="button"
                        data-bs-toggle="collapse"
                        data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">

                    <ul class="navbar-nav me-auto">

                        <li class="nav-item">
                            <a class="nav-link @(controller == "Home" && action == "Index" ? "active" : "")"
                               asp-controller="Home" asp-action="Index">
                                <i class="fa-solid fa-house me-1" aria-hidden="true"></i> Home
                            </a>
                        </li>

                        @if (isAuth && User.IsInRole("Mechanic"))
                        {
                            <li class="nav-item">
                                <a class="nav-link @(controller == "AssignedJobs" ? "active" : "")"
                                   asp-controller="AssignedJobs" asp-action="Index">
                                    <i class="fa-solid fa-list-check me-1" aria-hidden="true"></i> Assigned Jobs
                                </a>
                            </li>
                        }
                        @if (isAuth && User.IsInRole("Customer"))
                        {
                            <li class="nav-item">
                                <a class="nav-link @(controller == "Customer" ? "active" : "")"
                                   asp-controller="Customer" asp-action="Orders">
                                    <i class="fa-solid fa-box-open me-1" aria-hidden="true"></i> My Orders
                                </a>
                            </li>
                        }
                        @if (isAuth && User.IsInRole("Admin"))
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle @(area == "Admin" ? "active" : "")"
                                   role="button"
                                   data-bs-toggle="dropdown">
                                    <i class="fa-solid fa-user-shield me-1" aria-hidden="true"></i> Admin
                                </a>
                                <ul class="dropdown-menu">
                                    <li>
                                        <a class="dropdown-item"
                                           asp-area="Admin"
                                           asp-controller="Users"
                                           asp-action="Index">
                                            Manage Users
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item"
                                           asp-area="Admin"
                                           asp-controller="Jobs"
                                           asp-action="Index">
                                            Job Templates
                                        </a>
                                    </li>
                                </ul>
                            </li>
                        }

                    </ul>

                    <div class="d-flex">
                        <partial name="_LoginPartial" />
                    </div>

                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="site-footer">
        &copy; @year - MechTrack
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>